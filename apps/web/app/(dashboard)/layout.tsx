export const metadata = {
  title: 'EMS Dashboard',
  description: 'Equipment Management System Dashboard',
}

export default function DashboardLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children;
}
